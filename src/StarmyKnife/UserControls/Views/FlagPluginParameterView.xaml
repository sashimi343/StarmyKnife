<UserControl x:Class="StarmyKnife.UserControls.Views.FlagPluginParameterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:StarmyKnife.UserControls.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Style="{StaticResource BaseUserControlStyle}"
             mc:Ignorable="d" d:DesignHeight="100" d:DesignWidth="200"
             d:DataContext="{d:DesignInstance vm:FlagPluginParameterViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" ToolTip="{Binding HelpText}" ToolTipService.IsEnabled="{Binding HasHelpText}">
            <TextBlock Text="{Binding Name}" FontWeight="Bold">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="TextDecorations" Value="{x:Null}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasHelpText}" Value="True">
                                <Setter Property="TextDecorations" Value="{x:Static TextDecorations.Underline}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Label>
        <CheckBox Grid.Column="1" IsChecked="{Binding IsChecked}" />
    </Grid>
</UserControl>
